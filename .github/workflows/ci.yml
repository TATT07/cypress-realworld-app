name: CI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      # Start backend server
      - name: Start backend
        run: |
          nohup yarn start:api &
        shell: bash

      # Start frontend dev server
      - name: Start frontend
        run: |
          nohup yarn start:react &
        shell: bash

      # Wait until frontend is ready
      - name: Wait for server
        run: npx wait-on http://localhost:3000

      # Run unit tests
      - name: Run unit tests
        run: yarn test:unit

      # Run Cypress tests (FORCED TO USE THE CORRECT CONFIG FILE)
      - name: Run Cypress tests
        run: yarn cypress:run --config-file cypress.config.js

      # Save screenshots if tests fail
      - name: Upload artifacts (screenshots)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-screenshots
          path: cypress/screenshots

      # SonarCloud analysis
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=TATT07_cypress-realworld-app
            -Dsonar.organization=TATT07
